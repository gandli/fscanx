# BL-001 添加 -dp 命令行选项

## 结果

编译通过（`go build` + `go vet`），改动范围 3 个文件。

### 修改文件

- `common/config.go` — 添加 `DomainPortBind bool`
- `common/flag.go` — 添加 `-dp` flag
- `common/ParseIP.go` — 修改 `ReadInputFile` 域名处理 + 新增 `ProcessDomainAsURL`

### 风险

无。改动仅新增代码路径，不修改已有逻辑。

### 下一步

验收通过后关闭。
